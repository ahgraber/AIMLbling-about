# Experiments

Each directory is a discrete experiment with its own python environment managed by `uv`.

To activate the environment (from the repo root), run

```sh
uv sync --project experiments/<dirname>
```
